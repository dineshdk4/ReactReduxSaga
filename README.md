Change Path to Project root folder
Execute command to install nodu_modules
# npm i (or) npm install
once it done Execute command to start the application
# npm start

Application will run at localhost:3000
